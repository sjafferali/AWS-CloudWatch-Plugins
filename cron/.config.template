#
# Plugins Configuration
#
# = 0 - Disabled
# >= 60 - The interval in seconds at which to send stats to CloudWatch. [Minimum 60]
# --- 60 - Sends stats every minute to CloudWatch
# --- 3600 - Sends stats every hour to CloudWatch
# --- 86400 - Sends stats every day to CloudWatch


resources=0
mysql=0
nginx=0
lsync=0
mysqlslave=0
sns_topic=TOPIC
cluster=0
megaraid=0

